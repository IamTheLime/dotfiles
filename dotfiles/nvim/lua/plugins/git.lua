return {
    'dinhhuy258/git.nvim', -- For git blame & browse
    opts = {
        keymaps = {
            -- Open blame window
            blame = "<Leader>gb",
            -- Open file/folder in git repository
            browse = "<Leader>go",
        }
    }
}
